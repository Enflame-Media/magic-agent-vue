# pnpm configuration for NativeScript compatibility
shamefully-hoist=true
strict-peer-dependencies=false

# Use workspace protocol for local packages
link-workspace-packages=true
prefer-workspace-packages=true

# Faster installs
prefer-offline=true
